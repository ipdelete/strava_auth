# Your Strava app credentials (from https://www.strava.com/settings/api)
STRAVA_CLIENT_ID=your_client_id
STRAVA_CLIENT_SECRET=your_client_secret

# Redirect URI you configured in the Strava app (and used in setup_auth.py)
STRAVA_REDIRECT_URI=http://localhost:8080/exchange_token

# Leave these blank for now — script will populate after first run
STRAVA_ACCESS_TOKEN=
STRAVA_REFRESH_TOKEN=
STRAVA_EXPIRES_AT=
